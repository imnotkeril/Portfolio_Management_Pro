# Slim dependency set for the public Hugging Face Spaces backend.
#
# Excludes the heaviest / most build-fragile packages so the free-tier image
# builds reliably and cold-starts fast. The forecasting/report code imports
# these lazily, so the API boots fine and only the matching endpoints degrade:
#   - tensorflow      -> LSTM / TCN / SSA-MAEMD-TCN deep-learning forecasts
#   - EMD-signal      -> (needs tensorflow path)
#   - prophet         -> Prophet forecast family
#   - pmdarima        -> auto-ARIMA family
#   - playwright      -> headless-Chromium PDF path (WeasyPrint/ReportLab cover PDF)
# To enable them later, deploy from the full requirements.txt on a larger host.

# Core
numpy>=1.24.0
pandas>=2.0.0
scipy>=1.11.0
statsmodels>=0.14.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
email-validator>=2.0.0

# Database
sqlalchemy>=2.0.0
alembic>=1.12.0
psycopg2-binary>=2.9.0

# Auth
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
bcrypt>=4.0.1,<4.1.0
python-multipart>=0.0.9   # FastAPI form data (OAuth2 login)

# Data fetching
yfinance>=0.2.0
requests>=2.31.0

# Utilities
python-dotenv>=1.0.0

# Billing
stripe>=7.0.0

# API
fastapi>=0.115.0
uvicorn[standard]>=0.34.0
plotly>=5.17.0

# PDF generation (WeasyPrint/ReportLab path; no Playwright)
reportlab>=4.0.0
kaleido>=0.2.1
weasyprint>=60.0
img2pdf>=0.5.0
Pillow>=10.0.0

# Optimization
cvxpy>=1.4.0
ecos>=2.0.0

# Forecasting (lightweight families only)
arch>=6.2.0                 # GARCH
xgboost>=2.0.0              # XGBoost
scikit-learn>=1.3.0         # Random Forest, SVM
